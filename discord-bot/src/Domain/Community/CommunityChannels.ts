export enum CommunityChannels {
    SCREENSHOTS = 'screenshots',
    /**
     * Where per-run job summaries land (M6.8). Optional — see
     * DiscordChannels.ts for what happens when it is not configured.
     */
    ADMIN = 'admin',
}
